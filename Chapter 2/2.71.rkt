; in a tree where the relative frequencies are 1, 2, 4, ..., 2^(n-1) where n is
; the number of symbols. 1 bit is required to encode the most frequent symbol.
; n-1 bits are required to encode the least frequent symbol.

