; did the speed tests using (factorial 100) between analyzed expressions
; and non analyzed expression. 

; didn't seem to make much of a difference. but I'm measuring the time it
; takes in Chrome. I can only measure time, but not of cycles, so it's really
; hard to actually profile.
